version: 2
project_name: wahoo-pp-cli
changelog:
  disable: true
builds:
  - id: wahoo-pp-cli
    main: ./cmd/wahoo-pp-cli
    binary: wahoo-pp-cli
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w -X github.com/mvanhorn/printing-press-library/library/devices/wahoo/internal/cli.version={{ .Version }}
    targets:
      - darwin_amd64
      - darwin_arm64
      - linux_amd64
      - linux_arm64
      - windows_amd64
      - windows_arm64
  - id: wahoo-pp-mcp
    main: ./cmd/wahoo-pp-mcp
    binary: wahoo-pp-mcp
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w -X main.version={{ .Version }}
    targets:
      - darwin_amd64
      - darwin_arm64
      - linux_amd64
      - linux_arm64
      - windows_amd64
      - windows_arm64
archives:
  - formats: [tar.gz]
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    format_overrides:
      - goos: windows
        formats: [zip]
checksum:
  name_template: checksums.txt
brews:
  - name: wahoo-pp-cli
    repository:
      owner: stellato
      name: homebrew-tap
    homepage: "https://github.com/mvanhorn/printing-press-library"
    description: "Every Wahoo Cloud API capability, plus the local training database and offline analysis Wahoo's app never gives you."
    install: |
      bin.install "wahoo-pp-cli"
      bin.install "wahoo-pp-mcp"
