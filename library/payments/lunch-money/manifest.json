{
  "manifest_version": "0.3",
  "name": "lunch-money-pp-mcp",
  "display_name": "Lunch Money",
  "version": "4.5.2",
  "description": "Lunch Money API surface as MCP tools.",
  "author": {
    "name": "CLI Printing Press"
  },
  "license": "Apache-2.0",
  "server": {
    "type": "binary",
    "entry_point": "bin/lunch-money-pp-mcp",
    "mcp_config": {
      "command": "${__dirname}/bin/lunch-money-pp-mcp",
      "args": [],
      "env": {
        "LUNCHMONEY_API_KEY": "${user_config.lunchmoney_api_key}"
      }
    }
  },
  "user_config": {
    "lunchmoney_api_key": {
      "type": "string",
      "title": "LUNCHMONEY_API_KEY",
      "description": "Bearer token from https://my.lunchmoney.app/developers. The value is used directly as the bearer token; the CLI prepends Authorization: Bearer for you.",
      "sensitive": true,
      "required": true
    }
  },
  "compatibility": {
    "claude_desktop": ">=1.0.0",
    "platforms": [
      "darwin",
      "linux",
      "win32"
    ]
  }
}
