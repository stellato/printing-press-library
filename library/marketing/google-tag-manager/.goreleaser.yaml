version: 2
project_name: google-tag-manager-pp-cli
changelog:
  disable: true
builds:
  - id: google-tag-manager-pp-cli
    main: ./cmd/google-tag-manager-pp-cli
    binary: google-tag-manager-pp-cli
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w -X github.com/mvanhorn/printing-press-library/library/marketing/google-tag-manager/internal/cli.version={{ .Version }}
    targets:
      - darwin_amd64
      - darwin_arm64
      - linux_amd64
      - linux_arm64
      - windows_amd64
      - windows_arm64
  - id: google-tag-manager-pp-mcp
    main: ./cmd/google-tag-manager-pp-mcp
    binary: google-tag-manager-pp-mcp
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w -X main.version={{ .Version }}
    targets:
      - darwin_amd64
      - darwin_arm64
      - linux_amd64
      - linux_arm64
      - windows_amd64
      - windows_arm64
archives:
  - formats: [tar.gz]
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    format_overrides:
      - goos: windows
        formats: [zip]
checksum:
  name_template: checksums.txt
brews:
  - name: google-tag-manager-pp-cli
    repository:
      owner: stellato
      name: homebrew-tap
    homepage: "https://github.com/mvanhorn/printing-press-library"
    description: "Mirror any GTM container into a local database, then audit, diff"
    install: |
      bin.install "google-tag-manager-pp-cli"
      bin.install "google-tag-manager-pp-mcp"
