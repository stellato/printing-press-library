// Copyright 2026 Greg Stellato and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newDealsCreateCmd(flags *rootFlags) *cobra.Command {
	var bodyAudienceSegmentsTargeting string
	var bodyBeginDate string
	var bodyBuyerIds string
	var bodyCountryIds string
	var bodyCurrency string
	var bodyDealId string
	var bodyDomainCategorySegmentsTargeting string
	var bodyImpressionTypeIds string
	var bodyInventoryTypeIds string
	var bodyIsCTV bool
	var bodyIsDealPlus bool
	var bodyIsExclusive bool
	var bodyIsGreenPmp bool
	var bodyName string
	var bodyNetworkIds string
	var bodyPrice float64
	var bodyPricingModel int
	var bodyPriorityId string
	var bodySemanticSegmentsTargeting string
	var bodyTypeId string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "create",
		Short:       "Create a PMP deal (write; body shape approximate, verify live).",
		Example:     "  equativ-maestro-pp-cli deals create",
		Annotations: map[string]string{"pp:endpoint": "deals.create", "pp:method": "POST", "pp:path": "/deals"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !stdinBody {
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/deals"
			params := map[string]string{}
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyAudienceSegmentsTargeting != "" {
					body["audienceSegmentsTargeting"] = bodyAudienceSegmentsTargeting
				}
				if bodyBeginDate != "" {
					body["beginDate"] = bodyBeginDate
				}
				if bodyBuyerIds != "" {
					var parsedBuyerIds any
					if err := json.Unmarshal([]byte(bodyBuyerIds), &parsedBuyerIds); err != nil {
						return fmt.Errorf("parsing --buyer-ids JSON: %w", err)
					}
					body["buyerIds"] = parsedBuyerIds
				}
				if bodyCountryIds != "" {
					var parsedCountryIds any
					if err := json.Unmarshal([]byte(bodyCountryIds), &parsedCountryIds); err != nil {
						return fmt.Errorf("parsing --country-ids JSON: %w", err)
					}
					body["countryIds"] = parsedCountryIds
				}
				if bodyCurrency != "" {
					body["currency"] = bodyCurrency
				}
				if bodyDealId != "" {
					body["dealId"] = bodyDealId
				}
				if bodyDomainCategorySegmentsTargeting != "" {
					body["domainCategorySegmentsTargeting"] = bodyDomainCategorySegmentsTargeting
				}
				if bodyImpressionTypeIds != "" {
					var parsedImpressionTypeIds any
					if err := json.Unmarshal([]byte(bodyImpressionTypeIds), &parsedImpressionTypeIds); err != nil {
						return fmt.Errorf("parsing --impression-type-ids JSON: %w", err)
					}
					body["impressionTypeIds"] = parsedImpressionTypeIds
				}
				if bodyInventoryTypeIds != "" {
					var parsedInventoryTypeIds any
					if err := json.Unmarshal([]byte(bodyInventoryTypeIds), &parsedInventoryTypeIds); err != nil {
						return fmt.Errorf("parsing --inventory-type-ids JSON: %w", err)
					}
					body["inventoryTypeIds"] = parsedInventoryTypeIds
				}
				if cmd.Flags().Changed("is-ctv") {
					body["isCTV"] = bodyIsCTV
				}
				if cmd.Flags().Changed("is-deal-plus") {
					body["isDealPlus"] = bodyIsDealPlus
				}
				if cmd.Flags().Changed("is-exclusive") {
					body["isExclusive"] = bodyIsExclusive
				}
				if cmd.Flags().Changed("is-green-pmp") {
					body["isGreenPmp"] = bodyIsGreenPmp
				}
				if bodyName != "" {
					body["name"] = bodyName
				}
				if bodyNetworkIds != "" {
					var parsedNetworkIds any
					if err := json.Unmarshal([]byte(bodyNetworkIds), &parsedNetworkIds); err != nil {
						return fmt.Errorf("parsing --network-ids JSON: %w", err)
					}
					body["networkIds"] = parsedNetworkIds
				}
				if bodyPrice != 0.0 {
					body["price"] = bodyPrice
				}
				if bodyPricingModel != 0 {
					body["pricingModel"] = bodyPricingModel
				}
				if bodyPriorityId != "" {
					body["priorityId"] = bodyPriorityId
				}
				if bodySemanticSegmentsTargeting != "" {
					body["semanticSegmentsTargeting"] = bodySemanticSegmentsTargeting
				}
				if bodyTypeId != "" {
					body["typeId"] = bodyTypeId
				}
			}
			data, statusCode, err := c.PostWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Inspect the mutate response body for a partial-failure-shaped
			// field (e.g. Google Ads `partialFailureError`). Several Google
			// APIs return 200 OK with a partial-failure field when some
			// operations in the batch failed; ignoring it silently swallows
			// real failures. Detection runs before output-mode selection so
			// the exit code is consistent regardless of how stdout is
			// rendered. --dry-run short-circuits because no real request
			// was sent.
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
				if partialFailure != nil {
					fmt.Fprintf(os.Stderr, "warning: partial failure detected in %s response: %s\n", "deals", partialFailure.Message)
					if len(partialFailure.ResourceNames) > 0 {
						fmt.Fprintf(os.Stderr, "         succeeded: %d operation(s)\n", len(partialFailure.ResourceNames))
					}
				}
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "deals", data, "")
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						if partialFailure != nil && !flags.allowPartialFailure {
							return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "deals", partialFailure.Message))
						}
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							if partialFailure != nil && !flags.allowPartialFailure {
								return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "deals", partialFailure.Message))
							}
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					if partialFailure != nil && !flags.allowPartialFailure {
						return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "deals", partialFailure.Message))
					}
					return nil
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "deals",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure),
				}
				if partialFailure != nil {
					envelope["partial_failure"] = partialFailure
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				// Verify-mode synthetic envelope detection runs against RAW data
				// (before --compact/--select filtering) so the sentinel field is
				// guaranteed to be visible even if the operator passes a filter
				// flag that would otherwise strip it. Surfaces a top-level
				// verify_noop signal + flips success to false. Mirrors the dry_run
				// shape above.
				if len(data) > 0 {
					var rawParsed any
					if err := json.Unmarshal(data, &rawParsed); err == nil {
						if m, ok := rawParsed.(map[string]any); ok {
							if v, ok := m["__pp_verify_synthetic__"].(bool); ok && v {
								envelope["verify_noop"] = true
								envelope["success"] = false
							}
						}
					}
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				if perr := printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true); perr != nil {
					return perr
				}
				if partialFailure != nil && !flags.allowPartialFailure {
					return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "deals", partialFailure.Message))
				}
				return nil
			}
			// Fall-through for mutate paths that did not hit the table or
			// asJSON branches: --quiet, --csv, --plain, and default terminal
			// raw output. printOutputWithFlags renders the body, then the
			// typed partial-failure exit fires unless --allow-partial-failure
			// downgrades it. Without this guard a partial failure would exit
			// 0 for these output modes — the exact silent-swallow regression
			// the surrounding patch is preventing for asJSON / piped output.
			if perr := printOutputWithFlags(cmd.OutOrStdout(), data, flags); perr != nil {
				return perr
			}
			if partialFailure != nil && !flags.allowPartialFailure {
				return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "deals", partialFailure.Message))
			}
			return nil
		},
	}
	cmd.Flags().StringVar(&bodyAudienceSegmentsTargeting, "audience-segments-targeting", "", "Audience segments targeting")
	cmd.Flags().StringVar(&bodyBeginDate, "begin-date", "", "Deal start date.")
	cmd.Flags().StringVar(&bodyBuyerIds, "buyer-ids", "", "Targeted buyer (seat) ids.")
	cmd.Flags().StringVar(&bodyCountryIds, "country-ids", "", "Country ids")
	cmd.Flags().StringVar(&bodyCurrency, "currency", "", "ISO currency code, e.g. EUR.")
	cmd.Flags().StringVar(&bodyDealId, "deal-id", "", "Public deal ID trafficked in the DSP.")
	cmd.Flags().StringVar(&bodyDomainCategorySegmentsTargeting, "domain-category-segments-targeting", "", "Domain category segments targeting")
	cmd.Flags().StringVar(&bodyImpressionTypeIds, "impression-type-ids", "", "Impression type ids")
	cmd.Flags().StringVar(&bodyInventoryTypeIds, "inventory-type-ids", "", "Inventory type ids")
	cmd.Flags().BoolVar(&bodyIsCTV, "is-ctv", false, "Is ctv")
	cmd.Flags().BoolVar(&bodyIsDealPlus, "is-deal-plus", false, "Is deal plus")
	cmd.Flags().BoolVar(&bodyIsExclusive, "is-exclusive", false, "Is exclusive")
	cmd.Flags().BoolVar(&bodyIsGreenPmp, "is-green-pmp", false, "Is green pmp")
	cmd.Flags().StringVar(&bodyName, "name", "", "Deal name.")
	cmd.Flags().StringVar(&bodyNetworkIds, "network-ids", "", "Network ids")
	cmd.Flags().Float64Var(&bodyPrice, "price", 0.0, "Deal price (CPM in deal currency).")
	cmd.Flags().IntVar(&bodyPricingModel, "pricing-model", 0, "Pricing model id (e.g. fixed/custom/floor).")
	cmd.Flags().StringVar(&bodyPriorityId, "priority-id", "", "Priority id")
	cmd.Flags().StringVar(&bodySemanticSegmentsTargeting, "semantic-segments-targeting", "", "Semantic segments targeting")
	cmd.Flags().StringVar(&bodyTypeId, "type-id", "", "Type id")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
