// Copyright 2026 Greg Stellato and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newPartnersListCmd(flags *rootFlags) *cobra.Command {
	var flagIds string
	var flagName string
	var flagIsArchived string
	var flagIsDealCompliant string
	var flagCrossNetwork bool
	var flagSmartBuyerIds string
	var flagOffset string
	var flagLimit int
	var flagAll bool

	cmd := &cobra.Command{
		Use:         "list",
		Short:       "Returns all the RTB+ Partners",
		Example:     "  equativ-maestro-pp-cli partners list",
		Annotations: map[string]string{"pp:endpoint": "partners.list", "pp:method": "GET", "pp:path": "/partners", "mcp:read-only": "true"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if cmd.Flags().Changed("is-archived") {
				allowedIsArchived := []string{"False", "True", "Both"}
				validIsArchived := false
				for _, v := range allowedIsArchived {
					if flagIsArchived == v {
						validIsArchived = true
						break
					}
				}
				if !validIsArchived {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagIsArchived, "is-archived", allowedIsArchived)
				}
			}
			if cmd.Flags().Changed("is-deal-compliant") {
				allowedIsDealCompliant := []string{"False", "True", "Both"}
				validIsDealCompliant := false
				for _, v := range allowedIsDealCompliant {
					if flagIsDealCompliant == v {
						validIsDealCompliant = true
						break
					}
				}
				if !validIsDealCompliant {
					return fmt.Errorf("invalid value %q for --%s: must be one of %v", flagIsDealCompliant, "is-deal-compliant", allowedIsDealCompliant)
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/partners"
			data, prov, err := resolvePaginatedReadWithStrategy(cmd.Context(), c, flags, "auto", "partners", path, map[string]string{
				"Ids":             formatCLIParamValue(flagIds),
				"Name":            formatCLIParamValue(flagName),
				"IsArchived":      formatCLIParamValue(flagIsArchived),
				"IsDealCompliant": formatCLIParamValue(flagIsDealCompliant),
				"CrossNetwork":    formatCLIParamValue(flagCrossNetwork),
				"SmartBuyerIds":   formatCLIParamValue(flagSmartBuyerIds),
				"Offset":          formatCLIParamValue(flagOffset),
				"Limit":           formatCLIParamValue(flagLimit),
			}, nil, flagAll, "Offset", "offset", "Limit", "", "", cmd.ErrOrStderr())
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_promoted.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags.
			// --select wins over --compact when both are set; --compact only runs when
			// no explicit fields were requested. Explicit format flags (--csv, --quiet,
			// --plain) opt out of the auto-JSON path so piped consumers that asked for
			// a non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagIds, "ids", "", "Filter the results to return only the partners which have the matching ids. Separated by commas.")
	cmd.Flags().StringVar(&flagName, "name", "", "Filter by name of partners")
	cmd.Flags().StringVar(&flagIsArchived, "is-archived", "", "False if this partner can be used as parameter in other methods. True if deprecated / removed / deactivated... (one of: False, True, Both)")
	cmd.Flags().StringVar(&flagIsDealCompliant, "is-deal-compliant", "", "Filters the results according to the fact they can be used in a deal (one of: False, True, Both)")
	cmd.Flags().BoolVar(&flagCrossNetwork, "cross-network", false, "If set to true, partners from all networks will be returned")
	cmd.Flags().StringVar(&flagSmartBuyerIds, "smart-buyer-ids", "", "When specified, filter on partners associated to the given buyers")
	cmd.Flags().StringVar(&flagOffset, "offset", "", "Offset to start from")
	cmd.Flags().IntVar(&flagLimit, "limit", 0, "Maximum results to get")
	cmd.Flags().BoolVar(&flagAll, "all", false, "Fetch all pages")

	return cmd
}
