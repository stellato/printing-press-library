// Copyright 2026 Greg Stellato and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/mvanhorn/printing-press-library/library/marketing/equativ-maestro/internal/cliutil"
	"github.com/spf13/cobra"
)

func newDealsUpdateCmd(flags *rootFlags) *cobra.Command {
	var bodyApiCompliance string
	var bodyAudienceSegmentsDataCost float64
	var bodyAudienceSegmentsTargeting string
	var bodyBeginDate string
	var bodyBuyerIds string
	var bodyCompanyVendorCostRate float64
	var bodyCountryCodes string
	var bodyCountryIds string
	var bodyCreatedAt string
	var bodyCreatedBySupplyLegacyWorkflow bool
	var bodyCurrency string
	var bodyCustomVendorCost float64
	var bodyDealCriticalityId string
	var bodyDealId string
	var bodyDomainCategorySegmentsTargeting string
	var bodyGrossPriceCpmInDealCurrency float64
	var bodyId string
	var bodyImpressionTypeIds string
	var bodyInventoryTypeIds string
	var bodyIsActive bool
	var bodyIsArchived bool
	var bodyIsCTV bool
	var bodyIsCookieless bool
	var bodyIsDealPlus bool
	var bodyIsDisabledByRegulator bool
	var bodyIsDspSyncEnabled bool
	var bodyIsEditable bool
	var bodyIsExclusive bool
	var bodyIsGreenPmp bool
	var bodyIsParentDeal bool
	var bodyIsUnderDelivering bool
	var bodyKeywordIdsTargeting string
	var bodyManagementVendorCostRate float64
	var bodyName string
	var bodyNetworkIds string
	var bodyPrice float64
	var bodyPricingModel int
	var bodyPriorityId string
	var bodySemanticSegmentsDataCost float64
	var bodySemanticSegmentsTargeting string
	var bodySmartVendorCostRate float64
	var bodyTypeId string
	var bodyUpdatedAt string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:         "update",
		Short:       "Update a PMP deal (write; body shape approximate, verify live).",
		Example:     "  equativ-maestro-pp-cli deals update",
		Annotations: map[string]string{"pp:endpoint": "deals.update", "pp:method": "PUT", "pp:path": "/deals"},
		RunE: func(cmd *cobra.Command, args []string) error {
			if !stdinBody {
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/deals"
			params := map[string]string{}
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyApiCompliance != "" {
					var parsedApiCompliance any
					if err := json.Unmarshal([]byte(bodyApiCompliance), &parsedApiCompliance); err != nil {
						return fmt.Errorf("parsing --api-compliance JSON: %w", err)
					}
					body["apiCompliance"] = parsedApiCompliance
				}
				if bodyAudienceSegmentsDataCost != 0.0 {
					body["audienceSegmentsDataCost"] = bodyAudienceSegmentsDataCost
				}
				if bodyAudienceSegmentsTargeting != "" {
					body["audienceSegmentsTargeting"] = bodyAudienceSegmentsTargeting
				}
				if bodyBeginDate != "" {
					body["beginDate"] = bodyBeginDate
				}
				if bodyBuyerIds != "" {
					var parsedBuyerIds any
					if err := json.Unmarshal([]byte(bodyBuyerIds), &parsedBuyerIds); err != nil {
						return fmt.Errorf("parsing --buyer-ids JSON: %w", err)
					}
					body["buyerIds"] = parsedBuyerIds
				}
				if bodyCompanyVendorCostRate != 0.0 {
					body["companyVendorCostRate"] = bodyCompanyVendorCostRate
				}
				if bodyCountryCodes != "" {
					body["countryCodes"] = cliutil.SplitCSV(bodyCountryCodes)
				}
				if bodyCountryIds != "" {
					var parsedCountryIds any
					if err := json.Unmarshal([]byte(bodyCountryIds), &parsedCountryIds); err != nil {
						return fmt.Errorf("parsing --country-ids JSON: %w", err)
					}
					body["countryIds"] = parsedCountryIds
				}
				if bodyCreatedAt != "" {
					body["createdAt"] = bodyCreatedAt
				}
				if cmd.Flags().Changed("created-by-supply-legacy-workflow") {
					body["createdBySupplyLegacyWorkflow"] = bodyCreatedBySupplyLegacyWorkflow
				}
				if bodyCurrency != "" {
					body["currency"] = bodyCurrency
				}
				if bodyCustomVendorCost != 0.0 {
					body["customVendorCost"] = bodyCustomVendorCost
				}
				if bodyDealCriticalityId != "" {
					body["dealCriticalityId"] = bodyDealCriticalityId
				}
				if bodyDealId != "" {
					body["dealId"] = bodyDealId
				}
				if bodyDomainCategorySegmentsTargeting != "" {
					body["domainCategorySegmentsTargeting"] = bodyDomainCategorySegmentsTargeting
				}
				if bodyGrossPriceCpmInDealCurrency != 0.0 {
					body["grossPriceCpmInDealCurrency"] = bodyGrossPriceCpmInDealCurrency
				}
				if bodyId != "" {
					body["id"] = bodyId
				}
				if bodyImpressionTypeIds != "" {
					var parsedImpressionTypeIds any
					if err := json.Unmarshal([]byte(bodyImpressionTypeIds), &parsedImpressionTypeIds); err != nil {
						return fmt.Errorf("parsing --impression-type-ids JSON: %w", err)
					}
					body["impressionTypeIds"] = parsedImpressionTypeIds
				}
				if bodyInventoryTypeIds != "" {
					var parsedInventoryTypeIds any
					if err := json.Unmarshal([]byte(bodyInventoryTypeIds), &parsedInventoryTypeIds); err != nil {
						return fmt.Errorf("parsing --inventory-type-ids JSON: %w", err)
					}
					body["inventoryTypeIds"] = parsedInventoryTypeIds
				}
				if cmd.Flags().Changed("is-active") {
					body["isActive"] = bodyIsActive
				}
				if cmd.Flags().Changed("is-archived") {
					body["isArchived"] = bodyIsArchived
				}
				if cmd.Flags().Changed("is-ctv") {
					body["isCTV"] = bodyIsCTV
				}
				if cmd.Flags().Changed("is-cookieless") {
					body["isCookieless"] = bodyIsCookieless
				}
				if cmd.Flags().Changed("is-deal-plus") {
					body["isDealPlus"] = bodyIsDealPlus
				}
				if cmd.Flags().Changed("is-disabled-by-regulator") {
					body["isDisabledByRegulator"] = bodyIsDisabledByRegulator
				}
				if cmd.Flags().Changed("is-dsp-sync-enabled") {
					body["isDspSyncEnabled"] = bodyIsDspSyncEnabled
				}
				if cmd.Flags().Changed("is-editable") {
					body["isEditable"] = bodyIsEditable
				}
				if cmd.Flags().Changed("is-exclusive") {
					body["isExclusive"] = bodyIsExclusive
				}
				if cmd.Flags().Changed("is-green-pmp") {
					body["isGreenPmp"] = bodyIsGreenPmp
				}
				if cmd.Flags().Changed("is-parent-deal") {
					body["isParentDeal"] = bodyIsParentDeal
				}
				if cmd.Flags().Changed("is-under-delivering") {
					body["isUnderDelivering"] = bodyIsUnderDelivering
				}
				if bodyKeywordIdsTargeting != "" {
					body["keywordIdsTargeting"] = bodyKeywordIdsTargeting
				}
				if bodyManagementVendorCostRate != 0.0 {
					body["managementVendorCostRate"] = bodyManagementVendorCostRate
				}
				if bodyName != "" {
					body["name"] = bodyName
				}
				if bodyNetworkIds != "" {
					var parsedNetworkIds any
					if err := json.Unmarshal([]byte(bodyNetworkIds), &parsedNetworkIds); err != nil {
						return fmt.Errorf("parsing --network-ids JSON: %w", err)
					}
					body["networkIds"] = parsedNetworkIds
				}
				if bodyPrice != 0.0 {
					body["price"] = bodyPrice
				}
				if bodyPricingModel != 0 {
					body["pricingModel"] = bodyPricingModel
				}
				if bodyPriorityId != "" {
					body["priorityId"] = bodyPriorityId
				}
				if bodySemanticSegmentsDataCost != 0.0 {
					body["semanticSegmentsDataCost"] = bodySemanticSegmentsDataCost
				}
				if bodySemanticSegmentsTargeting != "" {
					body["semanticSegmentsTargeting"] = bodySemanticSegmentsTargeting
				}
				if bodySmartVendorCostRate != 0.0 {
					body["smartVendorCostRate"] = bodySmartVendorCostRate
				}
				if bodyTypeId != "" {
					body["typeId"] = bodyTypeId
				}
				if bodyUpdatedAt != "" {
					body["updatedAt"] = bodyUpdatedAt
				}
			}
			data, statusCode, err := c.PutWithParams(cmd.Context(), path, params, body)
			if err != nil {
				return classifyAPIError(err, flags)
			}
			// Inspect the mutate response body for a partial-failure-shaped
			// field (e.g. Google Ads `partialFailureError`). Several Google
			// APIs return 200 OK with a partial-failure field when some
			// operations in the batch failed; ignoring it silently swallows
			// real failures. Detection runs before output-mode selection so
			// the exit code is consistent regardless of how stdout is
			// rendered. --dry-run short-circuits because no real request
			// was sent.
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
				if partialFailure != nil {
					fmt.Fprintf(os.Stderr, "warning: partial failure detected in %s response: %s\n", "deals", partialFailure.Message)
					if len(partialFailure.ResourceNames) > 0 {
						fmt.Fprintf(os.Stderr, "         succeeded: %d operation(s)\n", len(partialFailure.ResourceNames))
					}
				}
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "deals", data, "")
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						if partialFailure != nil && !flags.allowPartialFailure {
							return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "deals", partialFailure.Message))
						}
						return nil
					}
				} else {
					var wrapped struct {
						Data []map[string]any `json:"data"`
					}
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							if partialFailure != nil && !flags.allowPartialFailure {
								return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "deals", partialFailure.Message))
							}
							return nil
						}
					}
				}
			}
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				if flags.quiet {
					if partialFailure != nil && !flags.allowPartialFailure {
						return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "deals", partialFailure.Message))
					}
					return nil
				}
				envelope := map[string]any{
					"action":   "put",
					"resource": "deals",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure),
				}
				if partialFailure != nil {
					envelope["partial_failure"] = partialFailure
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				// Verify-mode synthetic envelope detection runs against RAW data
				// (before --compact/--select filtering) so the sentinel field is
				// guaranteed to be visible even if the operator passes a filter
				// flag that would otherwise strip it. Surfaces a top-level
				// verify_noop signal + flips success to false. Mirrors the dry_run
				// shape above.
				if len(data) > 0 {
					var rawParsed any
					if err := json.Unmarshal(data, &rawParsed); err == nil {
						if m, ok := rawParsed.(map[string]any); ok {
							if v, ok := m["__pp_verify_synthetic__"].(bool); ok && v {
								envelope["verify_noop"] = true
								envelope["success"] = false
							}
						}
					}
				}
				// Apply --compact and --select to the API response before wrapping.
				// --select wins when both are set: explicit field choice trumps the
				// generic high-gravity allow-list. Otherwise --compact still applies
				// when --agent is on but the user did not name fields.
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				if perr := printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true); perr != nil {
					return perr
				}
				if partialFailure != nil && !flags.allowPartialFailure {
					return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "deals", partialFailure.Message))
				}
				return nil
			}
			// Fall-through for mutate paths that did not hit the table or
			// asJSON branches: --quiet, --csv, --plain, and default terminal
			// raw output. printOutputWithFlags renders the body, then the
			// typed partial-failure exit fires unless --allow-partial-failure
			// downgrades it. Without this guard a partial failure would exit
			// 0 for these output modes — the exact silent-swallow regression
			// the surrounding patch is preventing for asJSON / piped output.
			if perr := printOutputWithFlags(cmd.OutOrStdout(), data, flags); perr != nil {
				return perr
			}
			if partialFailure != nil && !flags.allowPartialFailure {
				return partialFailureErr(fmt.Errorf("partial failure in %s response: %s", "deals", partialFailure.Message))
			}
			return nil
		},
	}
	cmd.Flags().StringVar(&bodyApiCompliance, "api-compliance", "", "Api compliance")
	cmd.Flags().Float64Var(&bodyAudienceSegmentsDataCost, "audience-segments-data-cost", 0.0, "Audience segments data cost")
	cmd.Flags().StringVar(&bodyAudienceSegmentsTargeting, "audience-segments-targeting", "", "Audience segments targeting")
	cmd.Flags().StringVar(&bodyBeginDate, "begin-date", "", "Deal start date.")
	cmd.Flags().StringVar(&bodyBuyerIds, "buyer-ids", "", "Targeted buyer (seat) ids.")
	cmd.Flags().Float64Var(&bodyCompanyVendorCostRate, "company-vendor-cost-rate", 0.0, "Company vendor cost rate")
	cmd.Flags().StringVar(&bodyCountryCodes, "country-codes", "", "Country codes")
	cmd.Flags().StringVar(&bodyCountryIds, "country-ids", "", "Country ids")
	cmd.Flags().StringVar(&bodyCreatedAt, "created-at", "", "Created at")
	cmd.Flags().BoolVar(&bodyCreatedBySupplyLegacyWorkflow, "created-by-supply-legacy-workflow", false, "Created by supply legacy workflow")
	cmd.Flags().StringVar(&bodyCurrency, "currency", "", "ISO currency code, e.g. EUR.")
	cmd.Flags().Float64Var(&bodyCustomVendorCost, "custom-vendor-cost", 0.0, "Custom vendor cost")
	cmd.Flags().StringVar(&bodyDealCriticalityId, "deal-criticality-id", "", "Deal criticality id")
	cmd.Flags().StringVar(&bodyDealId, "deal-id", "", "Public deal ID trafficked in the DSP.")
	cmd.Flags().StringVar(&bodyDomainCategorySegmentsTargeting, "domain-category-segments-targeting", "", "Domain category segments targeting")
	cmd.Flags().Float64Var(&bodyGrossPriceCpmInDealCurrency, "gross-price-cpm-in-deal-currency", 0.0, "Gross price cpm in deal currency")
	cmd.Flags().StringVar(&bodyId, "id", "", "Internal numeric deal id (path id for GET/DELETE).")
	cmd.Flags().StringVar(&bodyImpressionTypeIds, "impression-type-ids", "", "Impression type ids")
	cmd.Flags().StringVar(&bodyInventoryTypeIds, "inventory-type-ids", "", "Inventory type ids")
	cmd.Flags().BoolVar(&bodyIsActive, "is-active", false, "Is active")
	cmd.Flags().BoolVar(&bodyIsArchived, "is-archived", false, "Is archived")
	cmd.Flags().BoolVar(&bodyIsCTV, "is-ctv", false, "Is ctv")
	cmd.Flags().BoolVar(&bodyIsCookieless, "is-cookieless", false, "Is cookieless")
	cmd.Flags().BoolVar(&bodyIsDealPlus, "is-deal-plus", false, "Is deal plus")
	cmd.Flags().BoolVar(&bodyIsDisabledByRegulator, "is-disabled-by-regulator", false, "Is disabled by regulator")
	cmd.Flags().BoolVar(&bodyIsDspSyncEnabled, "is-dsp-sync-enabled", false, "Is dsp sync enabled")
	cmd.Flags().BoolVar(&bodyIsEditable, "is-editable", false, "Is editable")
	cmd.Flags().BoolVar(&bodyIsExclusive, "is-exclusive", false, "Is exclusive")
	cmd.Flags().BoolVar(&bodyIsGreenPmp, "is-green-pmp", false, "Is green pmp")
	cmd.Flags().BoolVar(&bodyIsParentDeal, "is-parent-deal", false, "Is parent deal")
	cmd.Flags().BoolVar(&bodyIsUnderDelivering, "is-under-delivering", false, "Is under delivering")
	cmd.Flags().StringVar(&bodyKeywordIdsTargeting, "keyword-ids-targeting", "", "Keyword ids targeting")
	cmd.Flags().Float64Var(&bodyManagementVendorCostRate, "management-vendor-cost-rate", 0.0, "Management vendor cost rate")
	cmd.Flags().StringVar(&bodyName, "name", "", "Deal name.")
	cmd.Flags().StringVar(&bodyNetworkIds, "network-ids", "", "Network ids")
	cmd.Flags().Float64Var(&bodyPrice, "price", 0.0, "Deal price (CPM in deal currency).")
	cmd.Flags().IntVar(&bodyPricingModel, "pricing-model", 0, "Pricing model id (e.g. fixed/custom/floor).")
	cmd.Flags().StringVar(&bodyPriorityId, "priority-id", "", "Priority id")
	cmd.Flags().Float64Var(&bodySemanticSegmentsDataCost, "semantic-segments-data-cost", 0.0, "Semantic segments data cost")
	cmd.Flags().StringVar(&bodySemanticSegmentsTargeting, "semantic-segments-targeting", "", "Semantic segments targeting")
	cmd.Flags().Float64Var(&bodySmartVendorCostRate, "smart-vendor-cost-rate", 0.0, "Smart vendor cost rate")
	cmd.Flags().StringVar(&bodyTypeId, "type-id", "", "Type id")
	cmd.Flags().StringVar(&bodyUpdatedAt, "updated-at", "", "Updated at")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
